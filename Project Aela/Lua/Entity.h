// Entities are now in the 3D folder since they're basically what "Model3D" already was.
// In fact, I just renamed Model3D and changed it up. It works quite well.
// I need to work on them since they require materials, which is the other thing that I'm working on right now.
// -Robert

/*#pragma once

namespace Aela {
	class Entity {
	private:
		void init();
		void update();
	public:
		Entity();
		~Entity();
	};
}*/